"""Third-party code COPIED into this package, with its licence beside it.

One subpackage per upstream project, each carrying the upstream commit it was taken
from. Vendored files are not edited except where a copy cannot work otherwise (the
intra-package imports), and every such edit is listed in the subpackage's README.
"""
